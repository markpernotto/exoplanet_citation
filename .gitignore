# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.mypy_cache/
.ruff_cache/

# Virtual env
.venv/
venv/
env/

# Environment / secrets
.env
.env.local
.env.*.local

# dbt
target/
dbt_packages/
logs/
.user.yml
profiles.yml
# ...except our project-internal profiles.yml, which is parameterized via env_var()
# and contains no secrets. CI needs this file to be present.
!etl/transform/profiles.yml

# Node / Vite
node_modules/
dist/
.vite/
*.local

# OS
.DS_Store
Thumbs.db

# Editor
.idea/
.vscode/
*.swp
*.swo

# Snapshot data (lives in R2; only manifest is tracked)
data/snapshots/
data/archive/
data/unresolved.csv
!data/MANIFEST.jsonl

# Terraform
*.tfstate
*.tfstate.*
.terraform/
.terraform.lock.hcl

# Build artifacts
build/
*.egg

# Paper draft and LaTeX byproducts: kept private while in preparation.
# The public per-host data record lives in docs/cb_flag_audit.md, and the
# reproducibility script in etl/verify_cb_flag_paper.py is public.
docs/rnaas_cb_flag_audit.md
docs/rnaas_cb_flag_draft.md
docs/cb_flag_paper.tex
docs/cb_flag_paper.pdf
docs/cb_flag_paper.aux
docs/cb_flag_paper.log
docs/cb_flag_paper.out
docs/cb_flag_paper.bbl

# Grant/collaborator pitch and strategy notes. Kept private while seeking an
# institutional home.
docs/project_pitch.md

# Private working list for the "Could you live here?" survival feature (review menu).
docs/survival_feature_candidates.md

# Private planning list of next atmospheric-enrichment deep-dive targets.
docs/atmosphere_deep_dive_candidates.md

# Deep-dive tracker / enrichment playbook — kept out of new commits so the
# detailed/current tracker isn't published. (An older version is already in git
# history; that's fine and not being scrubbed.)
docs/citation_enrichment_playbook.md
